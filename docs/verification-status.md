# Verification status

This is a record of observed progress, not an acceptance certificate.

## August 29 IST (August 28 UTC): Codex subscription selected

The owner selected `gpt-5.6-luna` through the existing ChatGPT subscription. Official CLI `0.147.0` reports managed ChatGPT sign-in, Pro, available included quota, and zero extra credits. The bridge pins Luna, disables provider fallback and rejects paid/API-key modes, extra-credit balances and unknown quota state. It makes no purchase or account-billing changes. OpenRouter remains an explicit alternative; its idle gateway was stopped. No Ollama process or weights are used.

The real runtime check at `.local/runtime-verification-2026-08-28T18-52-42-236Z.json` passed actual sandbox execution returning 42, reconnection, one approved call, zero denied calls and stale-approval rejection. This establishes the subscription runtime path, not full repair acceptance.

The first Luna repair attempt under `.local/full-repair-b1044ff3-3227-485a-888a-30f4b99fa213/acceptance.json` reproduced the intended baseline fault. It then guessed the wrong staging command and retried; the unchanged runner rejected it as `EXACT_EXEC_REQUIRED`. No candidate was accepted. The bridge now separates the active instruction from history and encodes exact-command constraints before execution. A live regression exposed malformed nested JSON-string arguments; structured arguments fixed that format. The subsequent `.local/codex-exact-exec-regression.json` passed one actual exec call returning 42 with no repeat. Failures remain recorded separately.

Qodo's completed `c883e9b` review correctly identified manifest-only preflight and stale README status. Preflight now authenticates to the live gateway and checks its metadata; offline, unauthorized and wrong-model cases fail. Actual testing also accounted for TrueForge's deliberately masked credential listings by using the separate local capability. The README now distinguishes runtime success from pending repair and provider acceptance. Qodo's completed review of `4eeb69a` marks both findings resolved. Repeated historical findings remain addressed by the prior evidence below.

The current full suite passes **1,800 tests, zero failures/skips**, recorded in `.local/full-suite-codex-final.json`. TypeScript, ESLint and the operator production build pass. The initial restricted suite could not bind localhost; the authorized run passed. A synthetic subprocess mock initially looped on repeated exit events and exhausted its worker heap; its event model was corrected, cleanup was made idempotent and bounded, and the full suite passed afterward. No test assertions or oracle checks were weakened.

The second permitted repair attempt reused the same baseline, session, frozen oracle and attempt history. The new guard executed the exact staging command once and obtained a successful receipt. Luna then returned an empty acknowledgment, rejected as `CODEX_EMPTY_DECISION`; the coordinator recorded `RUNTIME_TURN_NOT_COMPLETED`. The result is `luna-command-guard-retry.json`, with the exact runtime events preserved separately. The output schema now requires a nonempty acknowledgment after an exact command, while the existing rejection remains. Both attempts are exhausted for this baseline; no history or counters were reset, and no candidate or full repair acceptance is claimed.

Qodo completed its `db98f94` review. Its additional OpenRouter consent-coverage finding was valid: the browser verifier now checks both hosted recipients in isolated contexts that intercept every API request, including the disabled submit state until consent. All ten browser check groups pass. Its date concern came from comparing the IST heading with UTC receipts; the heading now states both dates. The 189 independent repair/publication tests also pass with no failures or skips. The repeated turn-ordering, artifact-metadata, preflight-replay and missing-entrypoint claims remain contradicted by the source and executable checks documented here.

The full Luna controller workflow passed for run `99c255aa-7a0d-47e3-898e-65c1d966ee3c` against `db98f94`: all four scenarios passed API, browser and application-state checks, four real screenshots were saved, and both run-owned users were deleted. Its receipt is `.local/workflow-99c255aa-7a0d-47e3-898e-65c1d966ee3c/report.json`. The verifier now preserves each report and the previous successful repair seed, cancels unsuccessful runs, and validates every scenario and cleanup result instead of trusting the overall outcome label. This is local replay, not a Polar payment or webhook result.

The fresh fault fixture `.local/full-repair-40fb1cec-c265-46e7-9717-d53c4bd12c0f/` reproduced only the expected SC03 API/browser denial and passed all fourteen security controls. Attempt one failed before dispatch with `SANDBOX_OPERATION_FAILED`; subsequent read-only startup probes passed, so its intermittent cause is not claimed fixed. Attempt two reached source editing and wrote a candidate, but an oversized inspection response had been replaced by TrueForge with non-JSON guidance. The unchanged runner rejected `EXEC_RECEIPT_INVALID`; both failed attempts and the candidate's runtime history remain recorded. Neither failure is a verified repair.

The installed TrueForge response processor reproduced this format failure in three focused cases. A pinned core patch preserves the actual exit code, bounded output preview, explicit truncation flag, original byte count and output hash for recognized sandbox execution receipts. Invalid envelopes and MCP responses retain their existing behavior. Ten processor regressions and the existing sandbox tests pass; preparation now uses the known Node interpreter and bounded inspection instructions. No evaluator or receipt assertion was weakened. The complete suite after these changes passes **1,835 tests across 28 files, zero failures/skips**, recorded in `.local/full-suite-receipt-envelope.json`; TypeScript and ESLint pass. New tests remain implementation-aware.

The subsequent live probe caught that the initial patch covered CommonJS while the server loads ESM. Five added ESM cases reproduced the remaining failure; both shipped module formats are now patched and all twenty processor cases pass. The actual runtime regression at `.local/runtime-large-receipt.json` then passed two commands producing 40,000 bytes, with independently checked hashes and actual exit codes 0 and 17. A separate initial proposal was rejected before execution for violating the exact-command contract; that failed probe is retained. The final suite at this checkpoint passes **1,845 tests across 28 files, zero failures/skips**, recorded in `.local/full-suite-receipt-both-modules.json`. TypeScript and ESLint pass. This proves the receipt fix, not full generated-repair acceptance.

The `11f798e` fault fixture reproduced the baseline but its first edit turn ended with an empty model decision and no dispatched tool. The bridge now requests nonempty ordinary decisions and allows one replacement generation only for that empty-response case, preserving the same deadline and guards and accounting for both actual usage receipts. A regression also reproduced the coordinator incorrectly treating a terminal no-tool error as busy. Repair-only continuation now requires persisted proof that no tool was dispatched; missing, contradictory or uncertain histories still fail. The attempt limit and baseline are unchanged. The complete suite passes **1,854 tests across 28 files**, zero failures/skips, at `.local/full-suite-recovery.json`; TypeScript and ESLint pass. The pre-dispatch `REPAIR_RUNTIME_BUSY` retry report is preserved separately and did not consume an attempt.

The second attempt on that baseline successfully continued the terminal no-tool error and inspected actual source with a valid execution receipt. Its next model request failed with `CODEX_TURN_FAILED`, before any edit. Both attempts remain exhausted. A proposal-only diagnostic did not reproduce the connection failure, but exposed malformed nested JSON escaping in general `exec` arguments. Structured arguments and an explicit distinction between single-command acknowledgments and multi-step repairs fix those separate defects. All 81 gateway tests pass, including three new regressions that failed before the fix; a subsequent real Luna proposal passed the production parser without executing any tool. Its receipt is `.local/codex-repair-error-probe.json`. This is proposal-format evidence, not a verified repair or a claimed fix for the intermittent connection failure.

After restarting the worker with Luna selected, all nine actual UI browser check groups passed again, including the explicit OpenAI/subscription consent and stale-project configuration checks. The verifier created no project, run or provider objects. Codex protocol fixtures remain implementation-aware and are not presented as live product evidence.

## Latest UI and repair checks

The rebuilt operator workspace passes nine actual browser check groups, including exact report/clipboard bindings, keyboard tabs and history, 320/390/768-pixel layouts, connection recovery and isolated hostile presentation fixtures. The latest run also verifies `PROJECT_CONFIG_CHANGED` after restarting on a newer commit. A separate intercepted test first establishes a ready preflight, refreshes configuration and then reverts it: old approval state never becomes actionable. The UI offers a new connection without rewriting earlier project, run or approval bindings. No UI verification project, run or provider object was created. The receipt is `apps/web/.local/verification.json`.

The full suite passed **1,654 tests across 24 files, zero failures/skips**, with both the default worker configuration and one worker, recorded in `.local/full-suite-final-default.json` and `.local/full-suite-final-serial.json`. TypeScript, ESLint and the final operator production build passed. An earlier concurrent suite recorded two failures while the actual model workflow was running; the affected files then passed all 248 tests unchanged, followed by these complete passes. The failed report remains `.local/full-suite-receipt-limit.json`. No assertion or timeout was weakened. New tests remain implementation-aware.

The actual fault baseline in `.local/full-repair-48595c1f-e583-49d2-94e5-ba21576bc39f/` again reproduced only the scheduled-cancellation API/browser failure, with the other scenarios and all fourteen security controls passing. Its first generated-repair attempt returned prose without editing source, and was correctly rejected as `EMPTY_REPAIR_DIFF`. The next preparation supplies bounded previews of actual source plus recorded billing facts without fixture identities. Host tests and evaluator source remain unavailable to the model. Attempt two retained the same frozen oracle but failed during staging with `EXEC_RECEIPT_INVALID`. TrueForge replaced the oversized source-preview response with plain-text guidance, despite a zero exit code. The runner correctly rejected that non-JSON response. The fix caps serialized previews at 8 KiB, including escaping, below the measured runtime response threshold. Both failed attempts remain recorded; neither is reported as repaired.

Qodo's `fae5ee7` review confirmed the browser-budget and first-install fixes. Its new turn-ordering warning conflicts with both installed TrueForge database implementations, which explicitly use `created_at ASC, turn_id ASC`. SDK pagination consumes all pages, so the final item is the latest turn. The code was not reversed in response to that finding.

Qodo reviewed `b7d5e37` and correctly found stale ready-preflight state after a configuration refresh. The follow-up remounts project setup when its configuration changes and independently guards policy/run actions. The new real-browser fixture covers both a config change and a reversion. Qodo's completed review of `ed4cdd9` marks this finding resolved; the remaining repeated findings and their contrary evidence are recorded above.

`pnpm test:repair` exposes the actual isolated fault-injection workflow as a tracked script. The fresh baseline under `.local/full-repair-2f0284be-8e92-4ca8-86bf-a77d14442a95/` reproduced the intended failure and passed all fourteen security controls, but the coordinator rejected low disk capacity before consuming an attempt. Only the earlier, confirmed-inactive failed sandbox was removed; reports and the TrueForge database were retained. A private recovery script initially supplied the seed run ID instead of the new baseline ID and was correctly rejected before runtime dispatch. Its failure report remains separate.

Recovery against the actual baseline ID passed staging with a valid structured receipt and reached genuine model tool execution. At 16:21 UTC, a separate operator-authorized cleanup uninstalled Ollama and removed its model files while that turn was running. TrueForge recorded `status:error`, `message:terminated`; the coordinator abandoned the job with `RUNTIME_TURN_NOT_COMPLETED`. The failed report is `capacity-resume-correct-binding.json`, and `preparation-events.json` retains the source-inspection receipts. No candidate or before/after success is claimed. The executable and model directory were confirmed absent. Restoration is pending the operator's decision in that cleanup workflow; no reinstall, hosted fallback or paid model was started here.

## August 28: Ollama excluded from this Mac

The owner explicitly requested proceeding without Ollama. No restoration is pending or authorized. The new free hosted connection and its account/setup restrictions are documented in [Run without Ollama](free-model.md). A dedicated key was subsequently created with owner approval and a $0 limit. The authenticated metadata response reported `limit: 0`, `limit_remaining: 0`, `usage: 0`, `byok_usage: 0`, but `include_byok_in_limit: false`. Setup correctly refused activation before inference. The key editor had no control for that field; the documented update API requires a separate management credential. The new key is now disabled, confirmed in the UI, pending permission for that credential and completion of the cap. Existing keys and billing settings were untouched. No hosted inference or generated repair has been verified. Earlier local-model receipts remain historical evidence, not proof of the new connection.

The new implementation-aware gateway suite passes 67 tests, covering price changes, paid routing, key limits, credential separation, malformed requests, bounded streaming, disconnects and the exact TrueForge binding. TypeScript, ESLint and the operator production build pass. Setup exits without activation when its private key is missing.

After the actual key-creation attempt, all 67 gateway tests passed again without changing the implementation or assertions. The existing BYOK-exclusion case rejects before any generation request. This receipt is `.local/free-model-key-activation-tests.json`. The private key file is mode `0600`; no saved runtime selection or gateway capability was created by the rejected setup.

The final full suite passes **1,721 tests across 25 files, zero failures/skips**, recorded in `.local/full-suite-free-model-loopback.json`. The first run lacked socket permission. The authorized rerun exposed `EADDRNOTAVAIL` on local connections. A standalone HTTP probe reproduced it without any project code; explicitly binding its source to `127.0.0.1` returned HTTP 200. `TargetTransport` now applies that source binding only to already-authorized loopback destinations. DNS validation, external-network restrictions and every test assertion remain unchanged. Both failed suite reports are retained as `full-suite-free-model.json` and `full-suite-free-model-authorized.json`. No OS network setting was changed.

## August 28: free hosted runtime activated

The owner separately approved a temporary management credential. It updated only PaywallProof's dedicated key to `limit: 0`, `include_byok_in_limit: true` and enabled, with authenticated readback confirming zero remaining limit and zero usage. The management credential was immediately deleted through OpenRouter, then rejected with HTTP 401 when checked. Its private local file was removed. Existing keys and billing settings remained untouched. Receipts are `.local/openrouter-zero-cap-receipt.json` and `.local/openrouter-management-revocation.json`.

Gemma's free provider rejected the actual runtime probe and a minimal diagnostic with HTTP 429. Both failures remain recorded. An explicit configuration change selected `cohere/north-mini-code:free`, whose current catalog lists zero prices and tool support. The runtime alias is now `paywallproof-free/north-mini-code`. The gateway rejects the earlier Gemma ID, paid IDs and model fallback arrays.

The first North Mini Code turn executed Python successfully but its continuation failed with HTTP 400 because the gateway rejected the installed SDK's assistant `reasoning_content` field. The focused regression failed before the fix and passed afterward. Assistant reasoning text is now accepted under the same body-size cap; other roles, structured reasoning metadata and routing overrides remain rejected. Those test receipts are `.local/free-model-reasoning-before.json` and `.local/free-model-reasoning-after.json`.

The fresh actual check at `.local/runtime-verification-2026-08-28T18-26-39-324Z.json` passed sandbox execution returning 42, stream reconnection, one approved tool call, zero denied tool calls and stale-approval rejection. Post-inference account readback reports both usage counters at zero. No Ollama process, model weights, paid fallback or credit purchase was used.

The initial complete suite passes **1,726 tests across 25 files, zero failures/skips**, recorded in `.local/full-suite-free-model-activation.json`. TypeScript, ESLint and the operator production build pass.

The full hosted repair attempt at `.local/full-repair-7d5f49fb-1ff1-4a01-bdec-a5c5a7e92700/acceptance.json` completed the actual failing baseline. Only scheduled-cancellation access failed, in API and browser checks. The other scenarios and all fourteen security controls passed, and both disposable users were deleted. During repair attachment transfer, the model replayed the earlier baseline exec command and made thirteen further tool calls. The unchanged runner rejected the attempt as `UNEXPECTED_RUNTIME_TOOL`. The evaluator remained outside the model's checkout; no generated patch or verified repair is claimed.

An explicitly selected free GLM 5.2 endpoint returned HTTP 429 during its installation probe, before sandbox execution. Its failed receipt is `.local/runtime-verification-2026-08-28T18-33-31-363Z.json`. The configuration was then restored to North Mini Code, whose installation checks passed. No paid fallback, credit purchase, quota bypass or cap increase occurred. The final complete suite passes **1,727 tests across 25 files, zero failures/skips**, recorded in `.local/full-suite-free-model-final.json`. It adds rejection coverage for the unselected GLM ID. Full generated-repair and provider-lifecycle acceptance remain open.

## August 28 provider migration

Polar's isolated sandbox replaces Stripe. GitHub sign-in and sandbox organization creation completed. Separate worker and read-only reference tokens expire November 26, 2026. A private USD 10 monthly sandbox product was created. Both tokens passed actual organization/product/price preflight with API version `2026-04` and `X-Polar-Sandbox: 1`. Private receipts are under `.local/polar-sandbox-setup/`; no credentials or customer email are published.

No provider customer, checkout, payment or webhook lifecycle has succeeded. The sandbox rejected reserved example.com addresses with HTTP 422. The runtime then blocked sending a private owner-email alias to Polar because that data transfer needs specific authorization. It was not retried through another tool. No live payment, bank connection, paid service or credit redemption occurred.

Runtime, reference target, UI, policy/evidence schema v2, replay signing, repair packaging and active contracts have migrated. The Stripe SDK and adapter were removed. Native Polar webhooks use Standard Webhooks; internal replay uses a distinct route, key and header. Private checkout links are available only through the authenticated operator route. Unknown mutations are not blindly retried. Native cancellation waits for the real period boundary and provider state. Provider audit records are retained, not falsely marked deleted.

The final local suite passed **1,597 tests across 23 files, zero failures and zero skips** at `.local/full-suite-polar-migration-final.json`. TypeScript and ESLint passed. This includes 127 implementation-aware Polar boundary and native-webhook tests, browser bundle-size regressions, and thirteen added disk-capacity checks. Existing independent assertions remain; their mechanical schema/provider migration is disclosed in `tests/independent/README.md`. These are code-test results, not payment or full product acceptance.

Two real Next development JS bundles exceeded the browser transport's previous 1 MiB limit. That checkpoint allowed 4 MiB for static JS/CSS only. The later actual Webpack sandbox build produced a 12,235,561-byte entry; static JS/CSS now permit 16 MiB while API bodies keep 1 MiB. A further actual comparison isolated Next 16.3.3's development React debug channel, which waited on a websocket blocked by the verifier. Disabling only that debug channel fixed hydration under the unchanged destination restrictions.

## Verified local workflow

Run `58c1bdf7-c866-40a7-ab05-6adb2d007baf` executed through the actual TrueForge runtime and supported local Qwen3-4B-Instruct model against commit `fb4b1ac5737238612aba7ef54b439f258919aed1`. All four scenarios passed API, browser and application-state assertions, twelve passes total. Four actual browser screenshots were stored. Both run-owned application users were deleted during cleanup. The report is `.local/local-workflow-report.json` and the UI route is `/runs/58c1bdf7-c866-40a7-ab05-6adb2d007baf`. Its mode is `local_replay`; no Polar payment is claimed.

The preceding run `90a5d8ad-f8dc-4bf6-ab2c-b5ac5673d778` remains inconclusive, with eight API/state passes and four browser inconclusive results. Its report was preserved as `.local/local-workflow-before-hydration-fix.json`; it was not rewritten as a pass.

The actual repair-runner installation check also passed with the supported local model. TrueForge materialized hash-checked files, executed the trusted launcher, served two real sandbox HTTP responses through the reverse bridge and returned a successful exec receipt. `.local/final-runner-smoke-3.json` explicitly identifies this as installation evidence, not a generated application repair.

The embedded operator browser also needed the development React debug channel disabled. After that fix, actual token sign-in succeeded and the run's Scenarios and Report tabs displayed twelve passes, zero failures and the local-replay limitation. No network restriction was relaxed.

The new disk-capacity preflight ran against the actual source, artifact and runtime locations. Each had 408,768,512 available bytes, below the full-repair requirement of 4,294,967,296 bytes. It returned `REPAIR_DISK_CAPACITY_INSUFFICIENT` without starting a model or uploading files. `.local/repair-capacity-preflight.json` records this rejection. Tests cover exact thresholds, reserved blocks, separate volumes, invalid counters and repeated rejected requests without consuming repair attempts. This is a preflight, not a disk quota.

Qodo reviewed the rebuilt UI at `1e5cc72`. Its new first-install capacity finding was valid: a missing sandbox directory prevented its own initialization. The follow-up resolves only missing directories to a validated existing ancestor before checking capacity; other filesystem failures still reject. The screenshot finding is addressed by `artifactDownloadMetadata` at the controller lookup boundary, with authenticated before/after download, malformed provenance and corruption regressions. The missing-entrypoint finding is contradicted by the committed entrypoints and actual running development services. The preflight finding did not reproduce in four actual HTTP error/retry cases. Neither finding was dismissed solely because a test count increased.

## Remaining acceptance gates

- The real Polar paid checkout, actual webhook delivery and complete provider-backed SC01–SC04 remain unverified. Sending a private email alias needs the specific authorization required by the execution guard. No rejected transfer was bypassed.
- A full generated application repair and its before/after regression run remain unverified. The free hosted runtime is activated and passes actual execution and approval checks. Its full repair attempt failed because the model called tools during transfer; the stronger free endpoint returned HTTP 429. Preserve those failures and the unchanged evaluator. Do not restore Ollama, substitute a paid provider, increase the $0 cap or relabel a failed attempt.
- Qodo reviewed the latest implementation at `ed4cdd9` and confirmed the stale-preflight UI fix. No implementation merge or deployment has occurred.

## Earlier checkpoints and remaining work

- Source workspace initially contained only PRD.md.
- GitHub CLI account access verified. Public repository created with a documentation-only main branch. Work is on feat/paywallproof-foundation; no implementation merge has occurred.
- Local Node 22.20.0, pnpm 10.32.0, Docker 29.1.3, and an installed Ollama qwen3:4b model observed.
- A Stripe credit offer was located in the owner's email. Redemption and remaining balance are unverified. Private details are not stored in this repository.
- Stripe sandbox transactions do not move funds. No Stripe API call has run.
- Qodo installed on PaywallProof only after owner confirmation. Billing page shows a free trial and no active subscription. PR #1 received a completed Qodo review of the foundation commit. It correctly flagged development commands whose implementation files had not yet landed. Working entrypoints are now being added; review of the final implementation is pending. No merge has occurred.
- TrueForge 0.1.4 and SDK 0.1.3 installed locally with a lockfile. Server listens on 127.0.0.1:8790 with an isolated local database. Its startup probe reports local sandbox availability.
- A real local Ollama turn requested the sandbox exec tool. Tool execution failed during Python venv initialization. This is failed installation evidence, not a passing runtime integration.
- A pinned pnpm patch adds a narrow explicit bundled-Python override while preserving the local sandbox. A fresh TrueForge session executed `print(6 * 7)` inside its sandbox and returned an actual successful exec receipt containing 42. Deliberate SSE disconnection reattached to the same turn. A separate harmless MCP installation probe verified allow executes once, deny executes zero times, and stale approvals are rejected. This is installation evidence, not a full product run.
- Existing local Ollama qwen3:4b was retained. A second same-weight alias modified its prompt template and passed isolated sandbox execution probes. Later GGUF inspection identified Thinking-2507 weights, whose model card supports thinking mode only. The alias is not a supported non-thinking configuration. Its full product runs exhausted output limits or ended early. The official Qwen3-4B-Instruct-2507 Q4_K_M replacement is now downloaded and registered with TrueForge. Readback verifies Instruct weights, the unchanged official template, and a 32,768-token context. The first alias setup rejected an explicit Jinja template as Go syntax; inheriting the model's template and renderer together fixed setup. No hosted model or Daytona service is configured.
- Independent authors read only the PRD and public contracts, not implementation. Policy tests were run before their module existed, then passed unchanged. Control tests likewise failed for the missing module before implementation.
- Latest full-suite checkpoint, August 27: 1,446 tests across 21 files passed, with no skipped tests. TypeScript and ESLint passed. These include independent contract tests and explicitly labeled implementation-aware and synthetic integration tests. The restricted-shell attempt could not open test servers; the unchanged suite passed with local socket permission. This count is not product acceptance.
- Independent evidence tests exposed missing application-identity enforcement and incorrect free-state normalization. Independent HTTP tests exposed unchecked Host headers and validation response inconsistencies. Product code was fixed without weakening assertions.
- Reference Next.js production build and actual Playwright free/paid/canceled local-replay browser checks passed. Product UI production build passed. Full controller/TrueForge lifecycle and independent network/browser stress suite are in progress.
- The official Stripe CLI created an anonymous test sandbox using the owner's approved Git email. Its actual expiry is September 3, 2026. A read-only Stripe account identity request returned HTTP 403 with the temporary restricted key, so the product's Stripe preflight remains blocked. No Stripe lifecycle or transaction has run. Claiming the sandbox through Stripe is the supported retention route; it has not been claimed or extended. No country restrictions were bypassed, no Algora account was used, and no payment details were entered.
- August 28 claim attempt: after owner-completed mobile sign-in, Stripe listed only the existing Express account. The separate claim registration page showed an explicit India invitation requirement and disabled account creation. A request for free test-only access through October 28 is drafted privately but unsent. No provider expiry was changed; the local 60-day artifact setting is not a Stripe extension.
- Actual full-controller runs reached fixture preparation but ended before all scenarios. They were recorded as inconclusive and their owned fixtures cleaned up. Per-run MCP operation labels and a 32,768-token local context were corrected after observed failures. Explicit next-action responses are awaiting an actual full-lifecycle check.
- The first Instruct-model run reached approval, created its two users and attempted the browser probe. A response timeout rejected before its pending click was awaited, crashing the worker. A real-browser regression against incomplete synthetic HTML reproduced the unhandled rejection; attaching both promises to one awaited `Promise.all` fixed that failure path. After restart, the interrupted run finished inconclusive and both owned users were confirmed deleted. Its actual report is retained locally. The missing export action and a successful full lifecycle remain to be verified; the crash regression alone does not establish either.
- Screenshot storage has 100 independent integrity/expiry/path tests. Authenticated HTTP download and corruption rejection pass implementation-aware integration tests; no synthetic PNG fixture is presented as a browser screenshot.
- Qodo reviewed approved commit `9ae22ea` on public PR #1. Its repair screenshot finding was reproduced at the authenticated HTTP endpoint and fixed locally by validating complete repair provenance before adapting metadata for the strict download service. Regression tests cover original, before and after receipts, malformed provenance, run ownership and corrupt bytes. `pnpm dev` now selects 60-day local artifact retention; boundary tests preserve earlier explicit expiries and the direct worker's seven-day default. These follow-up changes have not been pushed or merged.
- Exact Git-source collection and installed-dependency packaging pass 88 independently authored tests, including real Git trees with case-colliding file and directory paths. The final repair sandbox runner passes its implementation-aware suite. Actual attachment upload and SDK readback match their hashes. Its latest real execution attempt stopped at the local model output limit before any exec call; no repair execution or passing repair is claimed.
- Independent timing tests caught a stale synchronization boundary and unawaited asynchronous authorization checks. Independent frozen replay tests caught malformed billing error normalization. These were fixed without weakening the tests.
- Repair orchestration now binds the original and patched runs to one frozen replay and an unchanged host oracle. Four lifecycle scenarios plus fourteen negative authentication, signature and ownership controls are required. The negative controls were run against actual local reference handlers, including deliberately broken handlers that exposed unauthenticated export or changed billing while returning a denial. No provider was called.
- Publication intent and decision recovery pass implementation-aware lost-response tests. Recovery looks up existing TrueForge continuations and never redispatches an uncertain request. These synthetic runtime responses do not establish a real publication approval or PR.
- Repair UI presentation has 41 focused tests. An actual browser check authenticated to the local worker, opened a persisted incomplete run, checked report/repair tabs and confirmed stop/repair actions were disabled for that terminal inconclusive run. No page console errors were observed. Generated-patch and publication views have not yet been checked with a real completed repair.
- Shared original-run and repair observation collection, sandbox orchestration, immutable verification and publication UI/backend paths are implemented. Actual model-generated repair, full Next sandbox execution, unchanged-oracle repair success and product PR publication remain unverified. The two specifically approved temporary research checkouts were verified clean and inactive, then removed to install the supported local model. Other checkouts and existing model weights were retained. Disk headroom remains limited; a larger repair sandbox run must wait for sufficient safe capacity.

## Required evidence

Each implementation increment records its tests, results, blocked checks, and Qodo review. Never count a skipped credentialed suite as passed. The acceptance catalogue in PRD.md remains mandatory.

## August 28 operator workspace rebuild

The operator UI now has a dark navigation rail, light workspace, responsive run history, full-history search, result filters and an attention queue. Run pages retain all scenario, finding, evidence, report, activity and repair controls. Tab deep links, browser history, keyboard navigation, labeled identifier/JSON copy controls and authenticated report downloads use the existing saved data and APIs. The PRD adds these requirements without removing scope.

The actual browser regression script verified token sign-in, saved-data counts, full-ID search, exact clipboard contents, filters, tab focus/history/reload, assertion-to-receipt agreement, a real JSON download, 320/390/768px layouts, mobile menu focus recovery, local read-only preflight and consent gates. A separate browser context intercepted every API request for empty/300-run/hostile-name/read-failure presentation fixtures. No fixture was persisted, and no new project, run or provider object was created. Browser receipts and actual screenshots are in ignored `apps/web/.local/`.

The full regression checkpoint after the UI rebuild and lazy-initialization fix passed 1,629 tests across 24 files with no failures or skips. TypeScript, ESLint and the operator production build also passed. Isolated acceptance repository copies under `.local` are excluded from test discovery to avoid duplicate test counts. New UI and failure-injection tests are implementation-aware; existing independent tests remain unchanged.

Qodo's claim that handled preflight errors leave unknown request outcomes did not reproduce. Four HTTP regression cases now verify exact durable status/body replay after invalid JSON, invalid mode, a missing project and a thrown preflight read error. The handler ran at most once; private error text stayed redacted. The actual Hono error handler resumes the middleware that records the response.

Storage cleanup explicitly authorized by the owner removed seven named unused app bundles while retaining Zoom and Telegram. It restored more than 11 GiB of available space at that checkpoint. The earlier 400 MiB blocker is historical, not the current state.

Full generated-repair acceptance is being exercised in an isolated Git copy with a deliberately injected access-policy error. This is an acceptance fixture, not a production finding. The actual run caught colocated test files shipped by Next and a dependency: the packager now excludes these while the sandbox still rejects test files. It also caught TrueForge's lazy sandbox initialization: a bounded one-byte attachment now initializes the sandbox only after a volume-capacity check, followed by validation of the actual recorded sandbox identity and root. Three targeted tests failed before that initialization fix and pass afterward. The full reference launcher subsequently failed during actual execution; no generated repair is claimed from those failed attempts. Fixed launcher stage diagnostics disclose no exception text or credentials.

The launcher failures then exposed two assumptions in its synthetic tests: SQLite 13 uses platform prebuilds, and Next 16.3.3 does not export `loadBindingsSync`. The launcher now checks the actual SQLite prebuild and initializes SWC through the exported synchronous transform, whose method names are checked against installed TypeScript declarations. This preserves the no-download preflight. Native loading was separately confirmed inside the actual sandbox; no filesystem permission was broadened.

Further actual attempts exposed descriptor exhaustion during route discovery, rejected Next asset version queries, a 12.2 MB Webpack entry, and process lifetime after server cleanup. Polling, a numeric-only asset version query, the bounded static-asset allowance and explicit foreground exit address those failures. A socket regression also reproduced premature target termination after an oversized response reset; reconnecting the existing bridge fixes it without retrying the rejected request. The added tests are implementation-aware. No oracle assertion, lifecycle expectation or independent test was weakened, and before/after evaluation must use the same oracle fingerprint.

The intermediate follow-up suite passed **1,646 tests across 24 files, zero failures/skips** at `.local/full-suite-ui-repair-followup.json`; TypeScript and ESLint passed. This checkpoint predates the larger static-asset and socket-reset changes. Failed runtime reports remain under ignored `.local/full-repair-*` directories. Only this turn's inactive runtime copies were removed after checking that no process used them; reports and receipts were retained.

The final code suite at `a44597b` passed **1,647 tests across 24 files, zero failures/skips**, recorded in `.local/full-suite-ui-repair-final.json`. TypeScript and ESLint passed. The large-body bridge test compares every byte using native `Buffer.equals`; it does not omit the body check or increase its deadline.

The actual isolated fault baseline at `.local/full-repair-beef9a6d-635a-4f31-8a38-424a4dd8c3db/failing-oracle.json` exited 1 as intended. SC01, SC02 and SC04 passed all channels; SC03 failed API and browser access while state passed. All fourteen security controls passed. Eight real screenshots were captured during collection, and both disposable users were deleted. TrueForge returned a successful launcher exec receipt. This baseline alone is not a verified repair.

That fixture's first repair attempt returned `EMPTY_REPAIR_DIFF`: the model inspected bare paths at the session root instead of the supplied checkout. Preparation now supplies exact prefixed paths and an explicit per-call working directory. Its second permitted attempt failed before any recorded transfer with `SANDBOX_OPERATION_FAILED`. Read-only runtime, turn-history, sandbox-identity and filesystem checks subsequently succeeded; that failure has not been reclassified as a pass. Both attempts remain recorded, and their two-attempt limit was not reset.

Qodo reviewed `a44597b`, resolved the first-install finding and identified unbounded concurrent asset forwarding. Browser probes now enforce 128 requests, two concurrent forwards and a 64 MiB aggregate byte budget, aborting active requests and queued work on breach. Real Chromium tests with hostile local HTML verify request-count and total-byte exhaustion, concurrency, an inconclusive result and absence of screenshot artifacts. The coordinator's own preflight also uses validated existing ancestors for not-yet-created artifact and sandbox directories.

The suite after these fixes passed **1,651 tests across 24 files, zero failures/skips**, at `.local/full-suite-browser-budget.json`. A subsequent pagination follow-up uses the SDK's documented one-item page limit when scanning attachment-bearing turn/session history. This bounds repeated history responses; it is not claimed as a reproduced explanation of the earlier transient startup failure. Focused repair/controller checks, TypeScript and ESLint passed after that follow-up.
